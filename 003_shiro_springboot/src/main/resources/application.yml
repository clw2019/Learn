server:
  port: 8000
  servlet:
    context-path: /springboot
spring:
  application:
    name: shiro_springboot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/003_shiro_springboot?serverTimezone=Asia/Shanghai
    username: root
    password: wj113
mybatis:
  type-aliases-package: com.clw.domain.*    #起别名
  mapper-locations: classpath:mapper/*.xml  #当mybatis的xml文件和mapper接口不在相同包下时，需要用mapperLocations属性指定xml文件的路径